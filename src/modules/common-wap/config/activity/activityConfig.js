import { baseUrl } from 'config/pageConfig';

export const activitiesUrl = { // eslint-disable-line
	multiple: `${baseUrl}promotions/multiple_bonus`,
	gift: `${baseUrl}promotions/free_gift`,
	firstDp: `${baseUrl}promotions/first_deposit`
};

// oddsAcitivty
export const oddsBoostActivity = `${baseUrl}promotions/odds_boost`;
